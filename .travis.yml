sudo: required

language: cpp


compiler:
  - gcc

arch:
  packages:
    - carla
    - kdebase-runtime
    - kparts
    - git
    - extra-cmake-modules
    - qt5-tools
    - python
    - okular-git
  script:
    - "git clone https://github.com/progwolff/performer.git"
    - "cd performer/dist/arch/ && makepkg -i"

script:
  - "curl -s https://raw.githubusercontent.com/mikkeloscar/arch-travis/master/arch-travis.sh | bash"
